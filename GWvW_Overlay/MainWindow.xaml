<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:local="clr-namespace:GWvW_Overlay" 
        mc:Ignorable="d" x:Name="MainWindow1" x:Class="GWvW_Overlay.MainWindow" Title="GWvW Overlay" 
        WindowStartupLocation="CenterScreen" WindowStyle="None" Height="{Binding Options.height}" Width="{Binding Options.width}" MinHeight="{Binding Options.height}" MinWidth="{Binding Options.width}"
        Foreground="{x:Null}" ResizeMode="CanResizeWithGrip" SizeToContent="WidthAndHeight"
        AllowsTransparency="True" Left="Auto" Topmost="True" Opacity="0.65" MouseDown="Drag" Loaded="onLoad">

    <ItemsControl ItemsSource="{Binding ObjectiveNames}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Canvas>
                    <Image Height="24" Width="24" Source="Resources/camp_blue.png" Stretch="Fill"/>
                    <Label Content="{Binding name}" Width="100" Canvas.Top="-20" Canvas.Left="-40" Height="36" Padding="0" HorizontalContentAlignment="Center" Foreground="White"/>
                    <Label Content="{Binding name}" Width="100" Canvas.Top="20" Canvas.Left="-40" Height="36" Padding="0" HorizontalContentAlignment="Center" Foreground="White" />

                    <Image Height="14" Canvas.Left="12" Canvas.Top="12" Width="12" Source="Resources/claimed2.png" Panel.ZIndex="60"/>
                    <!--<Label Content="{Binding OwnerData.OwnerName}" Height="36" Canvas.Left="8" Canvas.Top="55" Width="198" Padding="0" HorizontalAlignment="Left" HorizontalContentAlignment="Center" VerticalAlignment="Center"/>
                    <Label Content="{Binding OwnerData.LastClaim}" Height="22" Canvas.Left="8" Canvas.Top="10" Width="198" Padding="0" HorizontalContentAlignment="Center" VerticalAlignment="Center"/>
                    <Image Height="14" Canvas.Left="109" Canvas.Top="43" Width="12" Source="{Binding OwnerData.Claim}" Panel.ZIndex="60"/>-->
                </Canvas>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas>
                    <Canvas.Background>
                        <ImageBrush ImageSource="{Binding Options.active_map_img}" Stretch="Uniform"/>
                    </Canvas.Background>
                </Canvas>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="{x:Type ContentPresenter}">
                <Setter Property="Canvas.Left" Value="{Binding left}" />
                <Setter Property="Canvas.Top" Value="{Binding top}" />
            </Style>
        </ItemsControl.ItemContainerStyle>
    </ItemsControl>

</Window>
